# Schema scope

JSON Schema 2020-12 documents describe artifact payloads. The runtime `validatePayload` also checks dimension uniqueness, evidence requirements and verdict consistency. A JSON Schema pass alone is insufficient. No external JSON Schema engine was executed in this session. Fields derive from the inspected contracts; these documents are public interface descriptions, not private agent prompts.
